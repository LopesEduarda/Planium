export const goToPrices = (navigate) => {
    navigate(`/prices`)
}

export const goToPlans = (navigate) => {
    navigate(`/plans`)
}

export const goToBeneficiaries = (navigate) => {
    navigate(`/beneficiaries`)
}

export const goToAddBeneficiary = (navigate) => {
    navigate(`/addbeneficiaries`)
}

export const goToProposals = (navigate) => {
    navigate(`/proposals`)
}

export const goBack = (navigate) => {
    navigate('/')
}